import React from 'react';
import { Message } from 'semantic-ui-react';

const Error = () => {
    return (
        <div>
            <Message color="red">Customer(s) could't find</Message>
        </div>
    );
};

export default Error;